/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      // add other domains here if needed
    ],
  },
}

module.exports = nextConfig
